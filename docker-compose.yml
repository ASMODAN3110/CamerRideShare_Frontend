services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    depends_on:
      - backend

  # Adaptez cette section à votre backend (ex: Spring/Node/Nest/etc.)
  backend:
    image: backend:latest
    ports:
      - "3000:3000"

